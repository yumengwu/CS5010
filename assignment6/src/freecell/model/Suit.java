package freecell.model;

/**
 * This enum represent all the suits. Cards could have four suit: clubs (♣), diamonds (♦),
 * hearts (♥), and spades (♠).
 */
public enum Suit {
  clubs, diamonds, hearts, spades
}
